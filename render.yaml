services:
  # Backend API Web Service
  - type: web
    name: caresmart-backend
    env: node
    plan: starter
    buildCommand: npm install && npm run build --workspace=@caresmart/database && cd database && npx prisma generate && cd .. && npm run build --workspace=@caresmart/backend
    startCommand: cd database && npx prisma db push --skip-generate && npm run seed:demo && cd .. && node backend/dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        fromDatabase:
          name: caresmart-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: caresmart-redis
          property: connectionString
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FIELD_ENCRYPTION_KEY
        generateValue: true
      - key: PHONE_HASH_KEY
        generateValue: true
      - key: CORS_ORIGIN
        value: https://caresmart-frontend.onrender.com

  # Frontend Web Application (Static Site)
  - type: web
    name: caresmart-frontend
    env: static
    plan: starter
    buildCommand: npm install && npm run build --workspace=@caresmart/frontend
    staticPublishPath: ./frontend/dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://caresmart-backend.onrender.com/api/v1
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  # PostgreSQL 16 Database
  - name: caresmart-postgres
    plan: starter
    databaseName: caresmart_prod
    user: caresmart_admin

  # Redis Cache & Rate Limiting
  - type: redis
    name: caresmart-redis
    plan: starter
    ipAllowList: []
